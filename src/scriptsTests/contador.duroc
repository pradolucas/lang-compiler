programa

	declare NUMERO contador;
	declare STRING contadorTxt;
	contadorTxt := "Contador: ";

	leia(contador);
	escreva(contadorTxt);
	escreva(contador);
	
	contador := 5;
	
	while(contador<=10) {
    escreva(contadorTxt);
		escreva(contador);
    contador := contador + 1;
	};
	
	do
	{
	escreva(contadorTxt);
	escreva(contador);
  contador := contador - 1;
	}
	while(contador>=0);
	
fimprog.