programa

	declare NUMERO x, a, b, c, d.
	declare STRING txt, l.

	leia(b).
	leia(x).
	leia(l).
	escreva(b).

	x := 2.
	b := 300.
	
	se ( 2 >= 3) 
	entao { leia(a)} 
	senao {escreva(x)}.
	
fimprog.